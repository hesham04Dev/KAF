

import 'package:flutter/material.dart';

class two extends StatelessWidget {
  const two({super.key, required bool pla});

  @override
  Widget build(BuildContext context) {

    return const Placeholder();
  }
}
